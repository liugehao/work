WHERE 条件中不要使用 NOW() 等进行判断
========================================================================================================================================================================================================================

WHERE 条件中不要使用 NOW() 等进行判断，影响执行计划
